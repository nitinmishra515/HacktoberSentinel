{
  "name": "hacktober-sentinel",
  "version": "1.0.1",
  "description": "HacktoberSentinel GitHub Action to flag Hacktoberfest spam PRs.",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "pnpm exec ncc build index.js -o dist",
    "prepare": "pnpm run build",
    "test": "node --eval 'console.log(\"No tests yet\")'"
  },
  "keywords": [
    "github-action",
    "hacktoberfest",
    "spam"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
